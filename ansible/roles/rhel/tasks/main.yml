---
- name: Subscribe the host
  redhat_subscription:
    state: present
    username: "{{ rhn_user }}"
    password: "{{ rhn_password }}"
    autosubscribe: true
  when: os1_distro_variant == 'rhel'


- name: Install Basic Packages
  yum:
    name: "{{ item }}"
    state: latest
    #enablerepo: optional
  with_items:
    - git
    - vim
    - unzip
    #- yum-cron
